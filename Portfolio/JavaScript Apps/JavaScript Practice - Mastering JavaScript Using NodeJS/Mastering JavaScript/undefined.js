//undefined - In JavaScript undefined is used as the absence of a value

//NUll - In JavaScript, NULL is used by the user to define that here is no specific value.  

let name; 

console.log(name); 


if (name === undefined) {
    console.log('Please assign a value for the name'); 
} else {
    console.log('Welcome name!'); 
}


let square = function(num) {
    console.log(num); 
}

square(); // no argument was placed in the parameters hence the function of the program will set the value to undefined. 